#include "Question.h"

Question::Question(QString xImage, QString xQuestionContent, QString xCorrectAnswer, QString xAnswerA, QString xAnswerB, QString xAnswerC, QString xAnswerD)
:image(xImage), questionContent(xQuestionContent), correctAnswer(xCorrectAnswer), answerA(xAnswerA), answerB(xAnswerB), answerC(xAnswerC), answerD(xAnswerD)
{
    //Nothing to do yet.
}

Question::Question()
{
    //Nothing to do yet.
}
